# The realistic-signal version of i_replay_gate_oracle: coarse comes from the
# separately-trained coarse net, not ground truth. The honest deployable number.
run: {name: i_replay_gate_trained}
coarse: {source: trained}
conditioning: {mechanism: gate}
cl: {methods: [replay]}
