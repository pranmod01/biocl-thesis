# Frozen block-2 coarse features + gate (oracle superclass as inference log-prior).
# Stable features (no drift) + structural coarse (no cross-superclass interference).
run: {name: f_frozen_gate_oracle}
model: {backbone: frozen_coarse_block2, feature_dim: 4096}
coarse: {source: oracle}
conditioning: {mechanism: gate}
